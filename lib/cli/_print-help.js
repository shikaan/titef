const { print } = require('../_utils');

module.exports = () => {
  print(`Usage: titef [OPTIONS] [FILE]
Executes test files written in Titef framework.

Options:
  -h, --help            shows this help
  `);
};
